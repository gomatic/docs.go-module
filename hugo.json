{
  "baseURL": "https://gomatic.github.io/docs.go-module/",
  "title": "go-module",
  "publishDir": "_site",
  "enableRobotsTXT": true,
  "markup": {
    "goldmark": {
      "renderHooks": {
        "link": { "useEmbedded": "fallback" },
        "image": { "useEmbedded": "fallback" }
      }
    }
  },
  "params": {
    "description": "Public documentation for gomatic/go-module — parse a git remote URL into a Go module path and the name variants derived from it."
  }
}
